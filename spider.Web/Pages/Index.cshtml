@page
@using Microsoft.AspNetCore.Mvc.Localization
@using spider.Localization
@using Volo.Abp.Users
@using spider.Products;
@model spider.Web.Pages.IndexModel
@inject IHtmlLocalizer<spiderResource> L
@inject ICurrentUser CurrentUser

<div class="container">
    <div class="p-5 text-center">
        <h1>Ура, эта срань заработала</h1>
        <br/>
        @*@if (!CurrentUser.IsAuthenticated)
        {
            <a abp-button="Primary" href="~/Account/Login"><i class="fa fa-sign-in"></i> @L["Login"]</a>
        }*@

        <div class="col float-end">
            <form method="post" asp-page="IndexModel" asp-page-handler="Invoices">
                <button class="btn btn-outline-primary" style="position:fixed">Получить накладные</button>
            </form>
            <form method="post" asp-page="IndexModel" asp-page-handler="Routes">
                <button class="btn btn-outline-secondary" style="position:fixed">Пока НЗ</button>
            </form>
        </div>

        
        @await Html.PartialAsync("_partialInvoice",Model.invoices)
        
    </div>
</div>
